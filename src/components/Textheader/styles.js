import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  textDesign: {
    marginTop: '40%',
    fontSize: 20,
    fontWeight: '400',
    color: 'rgb(36,38,39)',
    textAlign: 'left',
  },
});
export default styles;
